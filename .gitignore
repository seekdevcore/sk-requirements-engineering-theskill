# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Local logs / drafts
*.log
*.bak
*.draft.md
DRAFT-*

# Python / generated
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# MCP server venv (uv.lock IS committed for reproducibility)
mcp-server/.venv/
mcp-server/.pytest_cache/

# Node / generated
node_modules/

# Anthropic Claude Code local state (per-skill)
.claude-cache/

# Local-only source material + working drafts — NEVER tracked.
# Holds the primary corpus (IFPB ERS course slides, Sommerville 10e, SBC ethics PDF)
# and maintainer working docs (plans, analyses). Excluded for two reasons:
#   1) Copyright — the slides/book are third-party material, not redistributable.
#   2) Size — the Sommerville 10e PDF alone is ~140MB.
docs/
