<!-- GENERIC TEMPLATE — copy to TX-NN-<slug>.md. A TX is cross-cutting technical/config/infra work NOT tied to a
     Feature/US (SKILL.md Rule 6). OpenProject: a "Task" parented to the "Apoio" child Epic. -->
# TX-NN — <cross-cutting technical task>

> **Type**: Task (cross-cutting `TX`)
> **Layer**: Backend | Infra | Frontend | Tooling   (drives the `[front]`/`[back]` tag)
> **Status**: ⏳ Pending | 🚧 In progress | ✅ Done
> **Sprint**: N

---

## What & why

<one line: the technical/config/infra work and why the project needs it. No Feature/US is served directly — if one is, this is a `TNN.M.K` inside that US instead.>

## Traceability

| Direction | Links to |
| --- | --- |
| ↑ motivated by | `<RNF-slug / ADR-NNN, if any>` |
| ↓ delivered by | `<commit / PR>` |

> A `TX` has no `CA` (it is not client-facing). Its "done" is the config/infra in place + the commit referenced above.
